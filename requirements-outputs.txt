# Output tooling dependencies (PDF / chart / Excel / QR generation).
# Install with: pip install -r requirements-outputs.txt
reportlab>=4.0
matplotlib>=3.7
pandas>=2.0
openpyxl>=3.1
qrcode[pil]>=7.4
pillow>=10.0
