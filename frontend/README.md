
  # Marine bunkering

  This is a code bundle for Marine bunkering. The original project is available at https://www.figma.com/design/2KXqE0040GF69hMpNTrzhz/Marine-bunkering.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  